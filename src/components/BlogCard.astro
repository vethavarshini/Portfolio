---
const {title,date,slug} = Astro.props;

//mark down 
---
<a href ={`/blog/${slug}`}>
<article class="flex flex-row items-center justify-between w-full">
    <h3 class="text-base text-black">{title}</h3>
    <p class="text-gray-500">{date}</p>
</article></a>